from django.apps import AppConfig


class CatalogoConfig(AppConfig):
    """App Django do catálogo técnico de produtos e especificações."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.catalogo"
